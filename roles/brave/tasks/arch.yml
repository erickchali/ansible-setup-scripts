---
- name: Install Brave Browser via paru
  ansible.builtin.command:
    cmd: paru -S --noconfirm --needed brave-bin
  register: brave_install
  changed_when: "'installing' in brave_install.stdout"
  failed_when: brave_install.rc != 0
